import { SpeakerNotes } from "~/components/SpeakerNotes";

export default function Chrome() {
	return (
		<div className="flex h-screen items-center justify-center gap-24">
			<SpeakerNotes>
				But the real promise of the web is that I can just send someone to my
				website - they don’t have to download anything special.
			</SpeakerNotes>
		</div>
	);
}
